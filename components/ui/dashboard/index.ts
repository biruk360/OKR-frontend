/**
 * Reusable dashboard primitives — used by /dashboard/reports (CEO + employee
 * super dashboards) and the per-user /dashboard. All components consume the
 * design tokens from tailwind.config.js / globals.css; do not add hex colors.
 */

export { DashboardCard } from './DashboardCard'
export { InsightTile } from './InsightTile'
export { KpiCard } from './KpiCard'
export { MiniBadge } from './MiniBadge'
export { Sparkline } from './Sparkline'
